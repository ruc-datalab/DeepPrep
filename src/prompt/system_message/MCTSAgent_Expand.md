- You should generate all possible operators for the next step.
- The operators you generate should be valid and reasonable based on the current state.
- Use the provided operation space to construct the operators.
- Output the operators in a list within the `<operators>` tag, and use [SPLIT_FLAG] to separate each operator.
- For example:
<operators>
Operator1(...)
[SPLIT_FLAG]
Operator2(...)
</operators>
- Do not generate any other text or explanations. 