You are dealing with a complex task with multiple rounds of interaction, don't try to complete it in one step.

- Output with tags below to answer the question.
    - <think> your_think_here </think>: This tag is used for analyzing what action should be taken next.
    - <operator> your_operator_chain_here </operator> or <solution> your_operator_chain_here </solution>
        - The operator chain are concatenated by " --> ", which will be executed by an external executor.
        - Please first use the <operator> tag to fully explore. During exploration, you can split the complicated task into multiple sub-tasks. Thus, you only need to generate several new operators at one turn. As shown in the demonstration, we only generate part of the operator chain at one turn.
        - If previous exploration results can deduce a correct operator chain to get the target table or it is the last turn, use the <solution> tag to summarize the correct operator chain. Output your final solution with operators used in the previous exploration. The solution must end with a Terminate operator.
        - IMPORTANT!: **For each turn, when generate operator chain, please generate from generate from scratch. You can refer to previous generated operator and execution results, but our system will not cache these execution results and assume the previous generated operators were executed.**
- Planning Before Execution:
    - Before generating operators, always analyze the task in <think> with the following steps:
        1. **Schema Mapping**: For each column in the target table, identify which source table(s) and column(s) it comes from. This helps you choose the correct join keys and avoid Cartesian products.
        2. **Function Identification**: List the high-level operations needed (e.g., join, union, groupby, pivot, filter) and their order.
        3. **Join Key Verification**: When joining tables, verify the join key by checking if the key column values in the preview data actually match between the two tables. If no matching values are visible, the join key may be wrong.
- Other Tips:
    - The table in the executing results will be truncated. Thus, when filtering the record, try to adopt multi-step fuzzy filtering to ensure the correctness of the filtering.
    - Carefully check the generated table with the target table schema description. DO NOT output redundant columns. Always use SelectCol at the end to keep only the target columns.
    - For most operators, the result table name remains the same as the source table name. For `join` and `union`, the result table name should follow the format `table_x_table_y_join` or `table_x_table_y_union`. 
    - **DO NOT** Generate the <observation> tag! **DO NOT** Generate the <observation> tag! **DO NOT** Generate the <observation> tag! Because the execution results will be given by an external executor.
    - **Join Tips**: By default, use "inner" join unless the task explicitly requires preserving all rows from one table. Inner join reduces the risk of Cartesian products and row count inflation. Always specify non-empty suffixes (e.g., ["", "_t2"]) in Join to avoid "columns overlap" errors. After Join, use SelectCol to keep only the target columns and remove any duplicate/extra columns from the right table. Verify the row count after join — if it is unexpectedly large, you may have a Cartesian product due to a wrong join key.
    - **Union Tips**: The input tables shown are TRUNCATED previews. The actual tables may contain many more rows. When the task requires combining data from multiple tables, you MUST include ALL input tables in the Union, not just the ones shown in the preview.
    - **Aggregation Tips**: When choosing aggregation functions in GroupBy or Pivot, cross-check with the provided target instance values. If the target instance value does NOT appear in the source data, it is likely computed via mean/average rather than max. Max values always come from the original data, while mean values can produce decimal values not present in the source. Keywords in column names also help: "average"/"avg"/"mean" → mean; "total"/"sum" → sum; "count"/"number" → count; "max"/"highest" → max.
    - **Last Column Tips**: When the target schema requires a sequence of columns (e.g., period1..period24, slot1..slotN), always verify the LAST column in the sequence exists in the source before attempting to rename it. Source tables may have fewer columns than expected (e.g., only 23 hourly columns instead of 24). If the source lacks the last column, use AddNewColumn to create it before Rename. NEVER assume a column exists without verifying it in the source data preview.
    - **Ignored Column Tips**: When the task description says a column "should be ignored", that column should NOT appear in your final output. Only include columns that are explicitly listed in the target column schema (the "Name of Column" list). Exclude all other columns from SelectCol.
    - **Datetime Format Tips**: When the target requires a specific datetime format, match it exactly by checking the target column instances. Do NOT automatically convert to a different format. If the target only needs the date part, strip the time portion. Use StandardizeDatetime with the correct date_format, or use AddNewColumn/SplitColumn to extract only the needed parts.
    - **Column Name Exact Match Tips**: Column names in the target schema must match exactly, including case sensitivity. Always copy column names verbatim from the target schema description.
    - **Row Count Verification Tips**: After GroupBy or Pivot, verify the row count matches expectations. If the target has fewer rows than the source, you likely need GroupBy. If the target has only 1 row but the source has many, you need to aggregate all rows into one (e.g., GroupBy with no by columns, or use a constant grouping key). Choose GroupBy keys that produce exactly the number of groups matching the target row count.
    - **Value Format Match Tips**: Not only column names but also cell values must match the target format exactly. Check the target column instances carefully: if the target shows values with a specific prefix or format (e.g., "Category A" instead of just "A"), you must output values in that same format. When using AddNewColumn to create values, always format them to match the target instances exactly.
    - **Column Selection Tips**: After Join, Union, or any operation that may introduce extra columns, always use SelectCol to keep only the columns specified in the target schema. The column order in SelectCol should match the target schema order.
- The capabilities you should have:
    - You should have the ability to effectively explore the operator space to find the correct operator chain.
    - You should have the ability to correct the previous wrong operator in the new exploration.
    - You should have the ability to examine the execution results to determine whether a correct target table is generated to determine whether a correct operator chain is found.

You will have {max_explore_turn} to explore. For each exploration, you should first reflect with the tag <think"> based on the previous <observation>, and then use <operator> tag to explore. After exploration, you must output a solution with tag <solution>. Make sure all operators in the <solution> can be executed successfully.
